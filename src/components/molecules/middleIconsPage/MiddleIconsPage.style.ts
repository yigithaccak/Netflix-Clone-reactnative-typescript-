import {Colors} from '../../../../Colors';
import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
  },
  listemContainerStyle: {
    backgroundColor: Colors.charcoal,
    opacity: 0.8,
  },
  listemIconStyle: {
    color: Colors.white,
    fontSize: 25,
  },
  listemTextStlye: {
    color: Colors.white,
    left: 3,
  },
});
