import {StyleSheet} from 'react-native';
import {Colors} from '../../../../Colors';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    bottom: 130,
  },
  textStyle: {
    color: Colors.cherry,
    fontSize: 24,
    marginLeft: 18,
    bottom: 90,
  },
});
