import {StyleSheet} from 'react-native';
import {Colors} from '../../../../Colors';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
bottom:95
  },
  textStyle: {
    color: Colors.white,
    fontSize: 20,
    marginLeft: 18,
    bottom: 100,
  },
});
