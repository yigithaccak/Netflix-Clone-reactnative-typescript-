import {StyleSheet} from 'react-native';
import {Colors} from '../../../../Colors';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',

  },
  textStyle: {
    color: Colors.white,
    fontSize: 20,
    bottom: 8,
    marginLeft: 18,
  },
});
