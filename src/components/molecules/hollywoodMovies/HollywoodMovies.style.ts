import {StyleSheet} from 'react-native';
import {Colors} from '../../../../Colors';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    marginTop: 20,
  },
  textStyle: {
    color: Colors.white,
    fontSize: 20,
    marginLeft: 18,
    top: 17,
  },
});
