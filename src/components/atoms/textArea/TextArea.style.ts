import {StyleSheet} from 'react-native';
import {Colors} from '../../../../Colors';

export const styles = StyleSheet.create({
  container: {
    width: 340,
    height: 150,
    bottom: 150,
    marginLeft: 62,
  },
  textOneStyle: {
    color: Colors.white,
    fontSize: 22,
    fontWeight: 'bold',
  },
  textStyle: {
    color: Colors.white,
    fontSize: 14,
  },
});
