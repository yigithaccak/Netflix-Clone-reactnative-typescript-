import {StyleSheet} from 'react-native';
import {Colors} from '../../../../Colors';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    bottom: 175,
    marginLeft: 210,
  },
  iconStyle: {
    fontSize: 30,
    color: Colors.white,
    marginHorizontal: 30,
  },
});
